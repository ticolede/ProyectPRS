# ProyectPRS

To run this proyect : 

A) Deploy to IIS (localhost) the Wcf Service under the name "WcfProyectPRS" and add the connected service to "ProyectPRS"
B) 
   B1) Run "ProyectPRS" through visual studio or
   B2) Under folder´s path "clientApp" create a terminal and run "npm run"
